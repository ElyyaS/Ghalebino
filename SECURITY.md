# امنیت

## محافظت‌های پیاده‌سازی‌شده

- **احراز هویت:** هش رمز عبور با scrypt؛ کوکی session با `httpOnly`، `sameSite=lax` و `secure` در تولید.
- **مجوزدهی سمت سرور:** تمام mutation ها نقش و مالکیت منبع را در سرور بررسی می‌کنند (IDOR).
- **دانلود محافظت‌شده:** `/api/downloads/[orderItemId]` مالکیت سفارشِ پرداخت‌شده را قبل از تحویل فایل بررسی می‌کند.
- **قیمت سمت سرور:** قیمت و کارمزد همیشه در سرور بازمحاسبه می‌شود؛ هیچ‌گاه از کلاینت گرفته نمی‌شود.
- **اعتبارسنجی ورودی:** با Zod در هر action.
- **جلوگیری از XSS:** React به‌صورت پیش‌فرض خروجی را escape می‌کند.
- **لاگ ممیزی:** عملیات مدیریتی/مالی مهم در `audit_logs` ثبت می‌شوند.
- **اسرار:** تنها از `process.env` در سرور خوانده می‌شوند؛ از `NEXT_PUBLIC_` فقط برای مقادیر غیرحساس استفاده شده است.
- **رازها هرگز در کلاینت:** فایل‌های سروری با `server-only` علامت‌گذاری شده‌اند.

## مدل تهدید و IDOR

- فروشنده A نمی‌تواند محصول فروشنده B را با تغییر ID ویرایش کند (بررسی `products.sellerId === seller.id`).
- کاربر عادی نمی‌تواند فایل محصول خریداری‌نشده را دانلود کند.
- کاربر عادی نمی‌تواند وضعیت سفارش را «پرداخت‌شده» کند.
- فقط مدیر می‌تواند محصولات را تأیید/رد کند، نقش کاربران را تغییر دهد یا برداشت‌ها را بررسی کند.

## موارد دمو/موک (به‌عمد)

- پرداخت با `MockPaymentProvider` (همیشه موفق) — برای محیط دمو.
- ایمیل با `MockEmailProvider` (لاگ کنسول + نمایش لینک بازیابی در حالت توسعه).
- ذخیره‌سازی فایل با `MockStorageProvider` (تولید فایل نمونه).

این موارد همگی پشت رابط‌های مشخص قرار دارند و برای تولید باید با سرویس‌های واقعی جایگزین شوند.
